streamlit>=1.40
pandas>=2.0
docling>=2.0
ocrmac>=1.0; sys_platform == "darwin"
easyocr>=1.7; sys_platform != "darwin"
psutil>=5.9; sys_platform != "darwin"
# Windows WinRT OCR (OS 내장 OCR 래퍼 — 설치 후 한국어 등 언어팩 필요)
winrt-Windows.Media.Ocr>=2.0; sys_platform == "win32"
winrt-Windows.Graphics.Imaging>=2.0; sys_platform == "win32"
winrt-Windows.Storage.Streams>=2.0; sys_platform == "win32"
winrt-Windows.Globalization>=2.0; sys_platform == "win32"
pypdfium2>=4.0; sys_platform == "win32"
# Tesseract Python 래퍼 (바이너리는 별도 설치 필요 — setup.bat 참고)
pytesseract>=0.3; sys_platform == "win32"
google-genai>=1.0
openai>=1.0
anthropic>=0.40
